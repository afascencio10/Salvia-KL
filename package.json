{
  "name": "salvia-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Pruebas Playwright (API + E2E) para SALVIA — ver .knowledge/5-standards/testing-strategy.md",
  "scripts": {
    "test": "playwright test",
    "test:api": "playwright test tests/api/",
    "test:e2e": "playwright test tests/e2e/",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  }
}
